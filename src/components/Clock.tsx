import React, {useState} from 'react';

const Clock = () => {
    let [timer, setTimer]= useState<number | undefined>(undefined)


    return (
        <div>

        </div>
    );
};

export default Clock;